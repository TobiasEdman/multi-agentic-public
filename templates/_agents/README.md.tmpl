# `.agents/` — agent activity directory

<!--
STUB. Materialised at `<target-repo>/.agents/README.md` by `agentic-task init`.

Plan: ~/Developer/agentic_workflow/docs/plans/tier2_multi_agent.md (v2, Track 1 #4 — file-lock convention)

Will document:
- Per-agent activity files (`.agents/<agent-id>.json`)
- File-lock semantics (claim before edit; release after commit)
- Schema reference → schema.json
-->

> Stub — see plan §Track 1 #4.
