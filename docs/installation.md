# Installation

> Stub. Plan: `~/Developer/agentic_workflow/docs/plans/tier2_multi_agent.md` (v2).

Will document:

1. `pip install -e .` (or `uv sync`) — installs the `agentic-task` CLI.
2. `agentic-task init` in the target repo — copies `templates/` content into the target's `AGENTS.md`, `.agents/`, `.github/workflows/`, and `.claude/agents/`.
3. Optional: install `hooks/*.sh` into `~/.claude/hooks/` for branch-per-agent + multi-vendor attribution.

Body to be authored in a separate session (Track 1 #2 in the plan).
